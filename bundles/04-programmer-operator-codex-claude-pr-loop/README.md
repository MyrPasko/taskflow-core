# Bundle 04: Programmer Operator + Codex + Claude PR Loop

Compatibility wrapper for `delegate` mode.

## Install

```bash
./install.sh /absolute/path/to/target-project
```

This wrapper delegates to `scripts/install-mode.sh delegate`.

The actual source of truth now lives in `core/`, `modes/delegate/`, `core/addons/claude-executor/`, and `core/addons/claude-pr-workflow/`.
