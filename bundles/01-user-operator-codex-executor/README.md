# Bundle 01: User Operator + Codex Executor

Compatibility wrapper for `precision` mode.

## Install

```bash
./install.sh /absolute/path/to/target-project
```

This wrapper delegates to `scripts/install-mode.sh precision`.

The actual source of truth now lives in `core/` and `modes/precision/`.
